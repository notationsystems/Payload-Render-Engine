<!--
  GENERATED by scripts/gen-docs.mjs - do not edit by hand.
  `npm run check` regenerates this file and fails if it differs.
  Read from: shared/ecosystem.mjs, shared/platform.mjs
-->

# Notation Systems - architecture and terminology

The apparatuses, the corpus lifecycle they span, and the rules they arrived at independently.

**Scope.** This describes the Payload Render Engine - a read-only
projection over a corpus it does not own. It is not a description of a
deployed Caravan, Tradewind or Landshark service, and nothing here is
evidence that one exists.

**MEASURED** sections are read from the registers when this file is
generated. **AUTHORED** sections are decisions, and live in the
generator so there is still one source.

## MEASURED - the apparatuses

| Apparatus | Presence | Stages | Basis |
| --- | --- | --- | --- |
| **Data Acquisition Channel** | `PRESENT` | acquisition, evidence | tree carries source; no HTTP surface to probe from here |
| **Payload OCR Agent** | `PRESENT` | perception | tree carries source; a library, not a service — nothing to probe |
| **Payload Terminal** | `PRESENT` | canonical, operator | serves an HTTP surface; this OS reads it through the projection service |
| **Payload Corpus Graph** | `DECLARED` | graph | the repository exists and contains only .git — no source, no commit on any branch reachable here |
| **PayLoad OS — Render Engine** | `OBSERVED` | projection, operator | this apparatus — you are reading its projection right now |
| **God's Eye View** | `PRESENT` | - | tree carries source; a separate browser application, not a service this OS reads |
| **Tradewind SCM** | `SCAFFOLD` | - | nineteen files, one commit reading "Initial commit from Webflow-Examples/hello-world-nextjs-minimal" — an unmodified starter |

## MEASURED - the corpus lifecycle

1. **ACQUISITION** - what reached us, from where, and under what method?
2. **PERCEPTION** - what does this artifact say — with its warrant intact?
3. **EVIDENCE** - is this admissible, and under which class?
4. **CANONICAL STATE** - what does the corpus hold as authoritative right now?
5. **GRAPH PLANE** - how do the entities relate, and along which edges?
6. **PROJECTION** - what is a disposable representation of that state?
7. **OPERATOR** - what is healthy, stale, awaiting authority, or blocked?

## MEASURED - what the apparatuses agree on, independently

### Unanswerable is an answer. A refusal is typed and carries its remedy.

Seen in: `daf`, `ocr`, `terminal`, `render-engine`.

OCR: Refusal(code, detail, remedy, stage). Terminal: the refused:* queue grouped by mechanism, one shared remedy per group. Render Engine: SCREAMING_SNAKE refusals at HTTP 200. DAF: admission-gate refusals that name the gate.

### Absence is typed and never collapses to null or zero.

Seen in: `ocr`, `terminal`, `render-engine`.

OCR: six absence reasons. Terminal: unobserved as a value kind in its own right. Render Engine: ABSENT-with-reason, and absence is not zero stated on every surface that could imply it.

### A model score is never promoted to a fact.

Seen in: `ocr`, `render-engine`.

OCR carries extractionConfidence, sourceReliability, evidenceStatus and verificationStatus as four separate fields and pins the last two. The Render Engine keeps mined candidates gold and never renders them as observed.

### A derived representation may not mutate the state it derives from.

Seen in: `daf`, `ocr`, `terminal`, `render-engine`.

Terminal (from the CANONICAL side, by the apparatus that owns the state): corpusTable.ts frames itself as "A PROJECTION over canonical state: no new number ... Never authoritative, never editable, never re-importable". Render Engine (from the RENDERING side): INV-6, mechanically checked by scripts/check-seam.mjs, and SEC-017 in the invariant ledger. OCR: no canonical writer exists in the package, audited over its own source tree. DAF: the projection chain and the evidence chain do not import each other. The data-platform doctrine states it a fourth time as "no serving projection writes canonical truth" - and none of the four was copied from another.

### A check is not trusted until a planted violation has made it fail by name.

Seen in: `daf`, `render-engine`.

DAF's cycle step 6: plant the violation and watch the check fail by name before trusting it. The Render Engine's security passes did exactly this, and one check failed to bite and had to be rewritten.

### Proposing, authorizing and acting are three separate things, and a surface may never let one read as another.

Seen in: `terminal`, `render-engine`, `ocr`.

The Terminal's authorization gate states RECOMMENDATION != AUTHORIZATION != EXECUTION in its own source and keeps them as separate objects. The Render Engine's control plane runs the same ladder as observed -> proposed -> approved -> dispatched, where a cell lights only from a recorded fact and DISPATCHED needs a delivered tender, never an authorization. The OCR Agent draws the same line one stage earlier: it proposes candidates with adjudication pinned to pending and may not resolve or verify. Three trees, three vocabularies, one distinction — arrived at independently, which is the strongest evidence available that it is structural rather than stylistic.

### Doctrine that restates the code is generated from it, so it cannot drift.

Seen in: `daf`, `render-engine`.

DAF generates docs/generated/DOCTRINE.md from architecture/*.yaml and diffs it as a gate. The Render Engine's invariant ledger is declared once in code, served, and checked for drift.

### Tamper-evidence is not consensus. A private chain cannot prove its owner did not rewrite it.

Seen in: `terminal`, `render-engine`.

Terminal: transparencyLog.ts argues it in its own header - "A blockchain solves CONSENSUS AMONG MUTUALLY DISTRUSTING WRITERS. Internally there is one writer, so consensus is free and its machinery is pure overhead ... a private chain where you control every node CANNOT PROVE YOU DID NOT REWRITE IT. The chain says so means we say so". Render Engine: the commitment manifest states on its own surface that it is tamper-evidence and NOT attestation, and that binding the root to a time or an identity requires a signature this projection cannot make. Both refuse the same shortcut, and both say why on the surface rather than in a design note.

### A verification that cannot complete fails closed. Nothing is truncated, softened, or silently repaired.

Seen in: `terminal`, `render-engine`.

Terminal: loadOperationsStore.ts persists workflow events as hash-linked JSONL and, on restart, "replays and verifies the entire chain. A partial or conflicting line refuses recovery instead of truncating history" - the failure mode chosen is refusing to start, not starting with less history. Render Engine: SEC-014 requires a cryptographic verification failure to fail closed, and the offline verifier proves it by failing a tampered record rather than reporting a warning.

## MEASURED - what they do not agree on

### Four apparatuses carry four partially-overlapping vocabularies for how a value came to be known.

*Severity: structural.* Owned by **substrate — not this surface**.

- DAF evidence class: asserted · computed · derived · measured
- OCR epistemic state: OBSERVED · EXTRACTED · INFERRED · RESOLVED · VERIFIED
- Terminal value kind: reported · observed · estimated · derived · inferred · computed · unobserved
- Render Engine ladder: Observation · DerivedMetric · MinedPattern · Hypothesis · Inference

**Why it matters.** derived and computed appear in three of the four and are not defined identically; observed, measured and OBSERVED are plausibly the same idea under three names; and reported — the single most common kind in the Terminal — has no counterpart anywhere else, even though a counterparty assertion is exactly what DAF calls asserted. A value crossing two apparatuses is being relabelled by hand at each seam, and a relabelling nobody wrote down is where provenance is lost.

**Proposal.** Treat DAF's four ingest classes as the canonical axis, because they are the only set fixed at ingest and refused on reassignment. Map Terminal reported to asserted, Terminal observed to measured, and keep estimated/inferred as a SECOND, orthogonal axis (how far from the evidence) rather than a competing value on the first. The OCR states are a third axis (how far through the pipeline) and should not be flattened into either.

### The notation:// identity space is specified and unimplemented.

*Severity: structural.* Owned by **substrate — not this surface**.

- DAF mints content-addressed evidence ids
- OCR mints content-addressed artifactIds
- Terminal mints entity ids of the shape ent:mine:escondida
- Render Engine reads Terminal ids and adds its own build fingerprints

**Why it matters.** One canonical identity space with many physical representations was the stated invariant. Today each apparatus mints in its own space and the joins between them are positional. Nothing is wrong yet because the joins are few; they stop being few at the first cross-apparatus query.

**Proposal.** The scheme already has a shape in docs/SECURITY.md §5 and the substrate directive. The first useful step is not a migration but a resolver: one apparatus that answers "what does this notation:// URI name, and which apparatus holds it" without anything having to renumber.

### The graph plane has a repository and no implementation.

*Severity: gap.* Owned by **substrate — not this surface**.

- notationsystems/payload-corpus-graph contains only .git

**Why it matters.** Entity-to-entity structure is currently computed per-read inside other apparatuses (the Render Engine mines it; the Terminal expands it). That is correct for now and stops being correct as soon as an edge needs to be evidenced and versioned in its own right, because there is nowhere to put it.

**Proposal.** Leave it DECLARED and visible. An empty repository that everyone can see is a better artifact than a graph half-built inside a projection layer that is forbidden to hold state.

## AUTHORED - terminology

- **Apparatus** - a repository that holds one stage of the corpus lifecycle.
- **Corpus** - a versioned, committed set of records with a proof root.
- **Projection** - a rebuildable read over a corpus. Never authoritative.
- **Limb** - which of the three kinds an answer is.
- **Plane** - who may read a route, and under what promise.
- **Product** - which of the three APIs owns a stream.
